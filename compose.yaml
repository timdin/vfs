version: '3.5'

services:
  mysql:
    image: mysql:5.7
    container_name: database
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: vfs
    ports:
      - 3306:3306